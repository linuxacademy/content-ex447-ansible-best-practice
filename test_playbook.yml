
---
- hosts: all
  tasks:
    - command: 'cat /etc/redhat-r*'
      register: cat1
    - command: 'tail /var/log/secure'
      register: cat2
    - debug:
       var: cat1
    - debug:
       var: cat2
